

//long start = System.currentTimeMillis(); //시작하는 시점 계산

//long end = System.currentTimeMillis(); 
//System.out.println( "실행 시간 : " + ((end - start)/1000.0) +”초”);



/*		Arrays.sort(pArr, (e1, e2) -> {
			if(e1[0] == e2[0]) {
				return e1[1] - e2[1];
			}
			else {
				return e1[0] - e2[0];
			}
		});
		
		좌표계의 Sorting을 위해 람다식을 활용해 비교연산을 바꿈.
*/
